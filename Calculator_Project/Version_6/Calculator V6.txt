Calculator V6 Requirements:

- allow the user to choose which operation they want
- allow the user to enter either a symbol or word for their operation
- Error handling for the input of numbers
- Using functions allow the user to use the calculate multiple times until they decide to leave the program
- Allow the user to store a calculation after they have performed it
- Allow the user to view their stored calculations
- Allow the user to enter a complex calculation